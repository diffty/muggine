#include "mathf.hpp"

int min(int a, int b) {
	return (a < b) ? a : b;
}

int max(int a, int b) {
	return (a >= b) ? a : b;
}

float minf(float a, float b) {
	return (a < b) ? a : b;
}

float maxf(float a, float b) {
	return (a >= b) ? a : b;
}
